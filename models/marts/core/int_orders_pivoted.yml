version: 2

models:
  - name: int_orders_pivoted
    description: "Intermediate pivoted orders model"
    columns:
      - name: order_id
        description: "Order identifier"
        data_tests:
          - relationships:
              arguments:
                to: ref('int_orders')
                field: order_id
